//for..in

const pessoa = {
  nome: 'weslley',
  idade: 18
}
//key-value
for (let chave in pessoa) {
  console.log(chave);
}

const cores = ['vermelho','amarelo','azul','verde'];

for (let indice in cores) {
  console.log(indice);
}